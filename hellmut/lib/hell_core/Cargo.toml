[package]
name    = "hell_core"
version = "0.1.0"
edition = "2021"
authors = [ "Lucas Muller <muller.lucas@mailbox.org>" ]

[dependencies]
axum = { version = "0.6.16", optional = true }

[features]
axum = ["dep:axum"]
